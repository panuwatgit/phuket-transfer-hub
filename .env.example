DATABASE_URL="postgresql://user:password@host:5432/phuket_transfer_hub"

# หลังบ้าน (เฟส 1 ผู้ใช้คนเดียว)
ADMIN_PASSWORD=""
AUTH_SECRET=""

# LINE Messaging API — ว่างไว้ = ไม่ส่งแจ้งเตือน (log แทน)
LINE_CHANNEL_ACCESS_TOKEN=""
LINE_CHANNEL_SECRET=""
LINE_ADMIN_USER_IDS=""            # comma-separated userId ของแอดมินที่รับ push
NEXT_PUBLIC_LINE_OA_ID="@024tyswy"

NEXT_PUBLIC_SITE_URL="http://localhost:3100"

# อีเมล (ไม่บังคับ) — Resend API key; ว่าง = log แทน
RESEND_API_KEY=""
EMAIL_FROM="Phuket Transfer Hub <onboarding@resend.dev>"

# ผู้รับเงินบนใบเสร็จ/ใบเสนอราคา
RECEIPT_ISSUER_NAME=""
RECEIPT_TAX_ID=""          # แสดงเฉพาะใบที่แอดมินเปิด "แสดงเลขผู้เสียภาษี"
RECEIPT_ADDRESS=""

# Google Places Autocomplete (ไม่บังคับ) — ว่าง = ใช้รายชื่อสถานที่ของเราอย่างเดียว
GOOGLE_MAPS_API_KEY=""

# LINE Login (ฟอร์มขั้น 3 "เชื่อม LINE") — channel แยกจาก Messaging API อยู่ provider เดียวกัน
LINE_LOGIN_CHANNEL_ID=""
LINE_LOGIN_CHANNEL_SECRET=""

# ตั้ง 0 ถ้าอยากให้ฟอร์มไทยส่งได้โดยไม่ต้องเชื่อม LINE
LINE_REQUIRED=""

# PromptPay สำหรับสร้าง QR ในแชท (เบอร์มือถือ 10 หลัก หรือเลขบัตร 13 หลัก)
PROMPTPAY_ID=""
PROMPTPAY_NAME=""
